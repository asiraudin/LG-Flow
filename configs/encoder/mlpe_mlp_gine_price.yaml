name: 'lpe'
num_vecs: 32
normalized_laplacian: no
normalize_eigenvecs: True
phi_model: 'mlp'
rho_model: 'gin'
pe_dim: 24
modulation: 0.
ds_dim: 16

phi:
  num_layers: 2
  hidden_dim: 256
  bias: True
  dropout: 0.

rho:
  num_layers: 16
  embed_dim: 256